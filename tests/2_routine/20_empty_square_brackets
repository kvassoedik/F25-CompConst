var globalVar: record var arr: array[5] integer end

routine func(a: array[] integer) is
    for i in 1..a.size-1 loop
        a[i] := i
    end
    for i in a loop
        print a[i]
    end
end

routine main() is
    func(globalVar.arr)
end